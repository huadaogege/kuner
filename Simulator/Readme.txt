1. build App for Simulator and launch it
  change App project to link libs of ffmpeg in dir ffmpeg-2.7-all

2. create img for simkuke with following command:
  $cd tjk/Simulator
  $make image

3. lauch simkuke
  $cd tjk/Simulator
  $ ./simkuke --ip=<iphone’s ip or Simulator’s(default: 192.168.2.1)>     

 
